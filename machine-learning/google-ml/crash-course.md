# 机器学习速成课程

## 机器学习模型

### 线性回归

学习目标：

- 解释损失函数及其运作方式。
- 定义并描述梯度下降如何找到最佳模型参数。
- 介绍如何调整超参数以高效训练线性模型。

在机器学习背景下，线性回归用于查找特征与标签之间的关系。

#### 线性回归方程

在机器学习中，线性回归模型的方程式如下所示：
$$y'=b+w_1x_1$$

其中：

- $y'$是预测标签
- b是模型的偏差。
- $w_1$是特征的权重。
- $x_1$是一个特征。

##### 具有多重特征的模型

$y'=b+w_1x_1+w_2x_2+w_3x_3+...+w_nx_n$

模型训练就是利用训练数据基于模型方程调整权重和偏差，使得模型方程更好地贴合数据。

#### 线性回归：损失函数

损失是一种数值指标，用于描述模型的预测与实际情况的偏差程度。损失用于衡量模型预测与实际标签之间的距离。训练模型的目标是尽可能降低损失，使其达到最低值。

##### 损失距离

在统计学和机器学习中，损失用于衡量预测值与实际值之间的差异。损失侧重于值之间的距离，而不是方向。

##### 损失类型

在线性回归中，有五种主要类型的损失，如下表所示。



### 逻辑回归

### 分类

## 数据

### 处理数值数据

### 处理分类数据

### 数据集、泛化和过拟合

## 参考链接

[Google ML Crash Course](https://developers.google.cn/machine-learning/crash-course)
